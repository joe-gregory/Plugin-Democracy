<nav class = 'navbar navbar-expand-lg'>
    <div class = 'container-fluid'>
        <a class = 'navbar-brand' href = '/'>DD LOGO</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <%- (locals.firstName == undefined) ? `<li class = 'nav-item'> <a class="nav-link" href='/signup'>Sign up</a></li>`:'' ;%>
                <%- (locals.firstName == undefined) ? `<li class = 'nav-item'> <a class="nav-link" href='/login'>Login</a></li>`:'' ;%>
                <%- (locals.firstName != undefined) ? `<li class = 'nav-item'> <a class="nav-link" href='/profile'>Profile</a></li>`:'' ;%> 
                <%- (locals.firstName != undefined) ? `<li class = 'nav-item'> <a class="nav-link" href='/community'>My Community</a></li>`:'' ;%> 
                <%- (locals.firstName != undefined) ? `<li class = 'nav-item navbar-brand> Welcome ${firstName}!</li>`:'' ;%> 
                <%- (locals.firstName != undefined) ? "<li class = 'nav-item'><form class = 'd-flex' action = '/logout' method = 'post'><button class='btn btn-light' type='submit'>Sign out</button></form></li>":'';%>
        </div>
            </ul>
            
    </div>
</nav> 